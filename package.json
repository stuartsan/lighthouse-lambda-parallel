{
  "name": "lighthouse-metrics",
  "version": "1.0.0",
  "main": "N/A",
  "repository": "https://github.com/stuartsan/lighthouse-metrics.git",
  "author": "Stuart Sandine",
  "license": "MIT",
  "scripts": {
    "lint": "eslint lambdas/",
    "fmt": "terraform fmt && prettier lambdas/**/*.js *.js --write",
    "install-all": "(cd lambdas/src/init && npm i) && (cd lambdas/src/post-processor && npm i) && (cd lambdas/src/worker && npm i)",
    "build": "(cd lambdas/src/init && npm run build) && (cd lambdas/src/post-processor && npm run build) && (cd lambdas/src/worker && npm run build)"
  },
  "devDependencies": {
    "eslint": "^5.13.0",
    "eslint-config-prettier": "^4.0.0",
    "eslint-plugin-import": "^2.16.0",
    "eslint-plugin-prettier": "^3.0.1",
    "prettier": "^1.16.4"
  },
  "dependencies": {}
}
